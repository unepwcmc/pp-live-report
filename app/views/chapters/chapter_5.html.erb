<%= render partial: 'partials/main-text', locals: { text: @data['content_1'] } %>

<map-statistics 
  id="<%= @map_1[:id] %>" 
  title="<%= @data['map_1_title'] %>" 
  description="<%= @data['map_1_description'] %>" 
  :tabs="<%= @map_1[:tabs].to_json %>"
  tiles-url="<%= @map_1[:tiles_url] %>" 
  source="<%= @data['map_1_source'] %>"
  disclaimer="<%= @shared_data['map_disclaimer'] %>"
>
</map-statistics> 

<%= render partial: 'partials/main-text', locals: { text: @data['content_2'] } %>

<!-- Not sure if second map is needed -->
 <map-statistics 
  id="<%= @map_2[:id] %>"
  title="<%= @data['map_2_title'] %>"
  description="<%= @data['map_2_description'] %>"
  :tabs="<%= @map_2[:tabs].to_json %>"
  tiles-url="<%= @map_2[:tiles_url] %>"
  source="<%= @data['map_2_source'] %>"
  disclaimer="<%= @shared_data['map_disclaimer'] %>"
>
</map-statistics> 

<section class="section-padding">
  
  <%= render partial: 'partials/component-title', locals: { title: @data['line_chart_title'] } %>

  <div class="container--medium">
    <chart-line 
      :lines="<%= @line_chart[:lines].to_json %>" 
      :targets="<%= @line_chart[:targets].to_json %>"
      :legend="<%= @line_chart[:legend].to_json %>"
      :axis="<%= @line_chart[:axis].to_json %>">
    </chart-line>

    <span class="smallprint"><%= @data['line_chart_smallprint'] %></span>
  </div>
</section>

<%= render partial: 'partials/main-text', locals: { text: @data['content_3'] } %>

<%= render partial: '/partials/cta', locals: { title: @data['cta_title'], text: @data['cta_text'], url: @data['cta_url'] } %>