<!DOCTYPE html>
<html>
  <head>
    <%= render 'partials/head' %>
  </head>

  <body class="<%= 'chapter-${chapter_number}' %>">
      <div id="v-app">
        <%= render 'partials/hero' %>
        
        <%= yield %>
      </div>

      <%= render 'partials/pagination' %>
      <%= render 'partials/footer' %>
  </body>
</html>
