<!DOCTYPE html>
<html>
  <head>
    <%= render 'partials/head' %>
  </head>

  <body>
      <div id="v-app">

        <%= render 'partials/hero-home' %>

        <%= yield %>
      </div>

      <%= render 'partials/footer' %>
  </body>
</html>
